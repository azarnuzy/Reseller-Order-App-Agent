import { modelConfig } from "@repo/config";
import { ORDER_AGENT_ID } from "./agent";

console.log(
  `${ORDER_AGENT_ID} workspace ready for Task 3 (${modelConfig.provider}/${modelConfig.model}).`,
);
